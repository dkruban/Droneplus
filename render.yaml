services:
  - type: web
    name: droneplus
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: droneplus-data
      mountPath: /opt/render/project/data
      sizeGB: 1
